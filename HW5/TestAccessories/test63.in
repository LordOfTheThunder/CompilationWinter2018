struct boolPotato {
	bool x;
	bool y;
};

void maxTeststestStructsWithTwoBooleans() {
	struct boolPotato t;
	t.x = true;
	t.y = false;
	if (t.x) {
		print("Success means doing the best we can with what we have. Success is the doing, not the getting; in the trying, not the triumph. Success is a personal standard, reaching for the highest that is in us, becoming all that we can be. – Zig Ziglar");
	}
}

void main() {
     maxTeststestStructsWithTwoBooleans();
     print("\nI hate this course\n");
}
